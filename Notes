AUC: use average of standardise rank

NN:
use binary file to load 
use time delta if not rnn friendly
use rnn with batch of 1

Feature engineering for time series:
  1. tool: big data sql instead of dask to create features
  2. type: count, cumcount (NA with mean), time delta (replace NA with max) (see TalkingData kernels)
  
